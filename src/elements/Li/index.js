import styled, { css } from 'styled-components';

import { buildStyledComponent } from 'utils';

const modifiers = {};

const styles = () => css``;

export default buildStyledComponent('Li', styled.li, styles, { modifiers });
